# 206. Autonomous agent runtime

The runner and Gateway attestation remain active. Simplify measured maintenance
or execution cost without adding a second executor or forcing an extraction.
Current size/complexity budgets belong to code_health_audit.py.

Preserve task/log DTOs, transport JSON, cancellation/retry, report hashes,
redaction and native action guards. An effective system_prompt.md can replace
the default prompt; inspect the loaded source and preserve user context.

Validate runner output/serialization, runtime startup and attestation on isolated
data. Never run synthetic writing attestation against production.
