# 011. Manager operations

Board scans, summaries, cleanup plans and verification share CardService with
ordinary board mutations. Simplify or separate these responsibilities without
copying validation, writes or audit/feed production.

Public compatibility routes and disabled reason codes are still contracts.
The embedded agent is active through desktop and MCP startup; a retired chat UI
does not establish that its runtime or enrichment is dead.

Check dry-run/apply revisions, unpaid/missing-data scans, private-data redaction,
actor/source attribution and event uniqueness. Use current capability/feed parity
for API/MCP integration; select structure from the actual dependency graph.
