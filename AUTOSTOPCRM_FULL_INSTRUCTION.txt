AUTOSTOP CRM
Short server operator note

Canonical runbook: docs/OPERATIONS_RUNBOOK.md

Runtime:
- checkout: /opt/autostopcrm
- branch: autostopcrm-v1
- public CRM: https://crm.autostopcrm.ru
- public MCP: https://crm.autostopcrm.ru/mcp
- compose services: autostopcrm, searxng, crawl4ai
- host data: /opt/autostopcrm/data
- container data: /root/.minimal-kanban
- host upstreams: 127.0.0.1:8000 -> API 41731; 127.0.0.1:8001 -> MCP 41831

Deploy only after the intended commit is on origin/autostopcrm-v1:
cd /opt/autostopcrm
./deploy.sh

The target branch is fixed by `CRM_DEPLOY_BRANCH=autostopcrm-v1` inside the
script. Do not pass a branch or Git-sync override; deployment requires a clean
checkout at exact remote parity.

Verify:
git status --short --branch
git rev-parse --short HEAD
git rev-parse --short origin/autostopcrm-v1
docker compose ps
docker compose exec -T autostopcrm python scripts/validate_production_env.py --require-production --require-store
docker compose exec -T autostopcrm python scripts/check_live_connector.py --strict --site-url https://crm.autostopcrm.ru --expect-https --local-api-url http://127.0.0.1:41731 --expect-admin
docker compose exec -T autostopcrm python scripts/check_agent_gateway_v2.py --mcp-url https://crm.autostopcrm.ru/mcp --exhaustive --require-store --require-web
docker compose exec -T autostopcrm python scripts/check_mcp_oauth.py --mcp-url https://crm.autostopcrm.ru/mcp
docker compose exec -T autostopcrm python scripts/docs_audit.py --format text

Diagnostics:
docker compose logs --tail=200 autostopcrm
curl -fsS http://127.0.0.1:8000/api/health
python3 scripts/state_size_report.py --json
python3 scripts/compact_audit_events.py --dry-run --json

Safety:
- keep .env, data, backups, active volumes, and dirty parallel checkouts
- never print tokens or full environment dumps
- never edit state.json, audit-archive, operator-activity, repair-order, or cashbox ledgers manually
- repair-order numbers are immutable; the compatibility correction route is blocked
- use dry-run and a verified backup before any approved maintenance write
- public anonymous API/MCP reads and all writes must stay blocked
- keep production OAuth enabled, development OAuth disabled, and the encrypted OAuth state key stable
- public clients must see exactly 24 Gateway v2 tools; use agent_bootstrap -> digest -> focused read -> action contract -> named dry_run/apply -> reread
